/**
 * Custom React hooks for Python Master 12
 * Re-exports from lib and components
 */

// Re-export useAuth from lib/auth
export { useAuth } from '@/lib/auth';

// Re-export useTheme from lib/theme
export { useTheme } from '@/lib/theme';

// Re-export useToast from components/common/UI
export { useToast } from '@/components/common/UI';